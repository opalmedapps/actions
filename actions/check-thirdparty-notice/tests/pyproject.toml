[project]
name = "actions"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "django==5.2.4",
]

[project.optional-dependencies]
rest = [
    "djangorestframework>=3.16.1",
]

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
